# 模芯设计（芯棒 / 内芯）—— `core_design.py`

面向 **薄壁中空件**（管件、型材件）的独立功能模块。
硬模（刚性模）成型时产品内孔是空的 —— 若不给内孔"撑腰"，成型/校形时管壁会被压塌。
本模块生成这根"撑腰"的**芯棒**（内芯 / mandrel）：

> **沿产品长轴的两个端口面，把产品内孔完整填满，生成一根芯棒实体。**

---

## 一、原理（复用管件模具里已验证的"封盖切断"思路）

> **先看这里：管件模具模式已经自带芯棒。**
> `main.py --mold-type pipe` 挖"外形包络"型腔时，内孔里那块"内芯条"本来就要被切断剔除 ——
> **它就是内孔腔 = 芯棒**，程序直接把它留下并导出**四件套装配体（上模 + 产品 + 芯棒 + 下模）**，
> **不额外花布尔时间**。本模块（`--mode core`）用于需要**单独出芯棒图**的场合：
> 端面与端口严格齐平、可留单边装配间隙、并逐站位校验填满/抽芯。
> 两者的差别只有端面位置（模具附带的那块端面差 0.1~0.5 mm）与校验详尽程度。

产品 `P` 是薄壁中空体，内孔在两端（端口面）开口。取一个把产品整个罩住的长方体 `C`（取芯块）：

```
C − P                  → 模具料 + 内孔腔（内孔腔经两端开口与外界连通）
C − P − 端口封盖薄片    → 把内孔腔与外界"切断"，内孔腔成为独立实体
取"不含取芯块角点"的实体 = 内孔腔 = 芯棒 ✅
```

两个关键点（与"挖型腔"的做法不同）：

| # | 做法 | 原因 |
|---|------|------|
| 1 | 封盖的**里面那一端必须落在"管壁仍然闭合"的截面上**，往外拉伸 | 这是本模块最关键的坑：端口若是圆角/斜切收口，最外点附近管壁已经结束，那儿的内孔腔**径向**与外界相通 → 切不断、取不出芯棒（实测正端漏 0.9 mm 时布尔结果里内孔腔仍连在主体上） |
| 2 | 封盖用截面**孔口轮廓**（沿轴向向外拉成棱柱），不是整个截面 | 与管件模具挖腔完全一致（已验证能可靠切断内腔）；封盖完全在产品之外/之内都只切"模料"，不会误切产品本体 |

截面站位用**二分法**往端口方向逼近（精度 0.02 mm），所以芯棒端面几乎就贴在真实孔口上。

端口定位的三个坑（实测踩到并已处理）：

* **不能用 `shape_bbox` 的极值当端口面**：`BRepBndLib` 会被 BSpline 控制点撑大
  （实测包围盒 757.49 → 真实几何 755.91）。端口位置取**采样点的真实几何极值**。
* **端口面可能是"圆角/斜切收口"**，没有垂直于长轴的平面端面（本例 +X 端：管壁到 755.0 结束、
  圆角外沿到 755.91，`planar_axis_faces` 查不到平面端面）。此时封盖站位取 754.995，
  芯棒端面与最外点差 0.91 mm，元数据 `ports[].flush_gap_mm` 会写明。
* **截面的环数判断**：`section_wires` 返回"按面积降序的闭合环"，`len(wires) >= 2` 表示
  "该站位管壁仍是闭合环（有内孔）" —— 这正是判断"管壁何时结束"的依据。

---

## 二、自动检验（结果全部写进 `{prefix}_metadata.json`）

| 检验 | 判据 | 说明 |
|------|------|------|
| **站位截面对比** | 每个站位上 芯棒截面面积 = 产品内孔截面面积 | 最直接：负偏差 = 没填满，正偏差 = 啃进管壁；21 个站位，约 40 s。**默认判据** |
| **质量守恒** | 取芯块 − 产品 − 封盖 = 模具主体 + 内孔腔 | 免费；布尔丢料/多料会立刻暴露（实测偏差 −0.0028%） |
| **空腔数** | 应为 1 件 | >1 说明产品内部有多个独立空腔（只取最大者并提示） |
| **抽芯干涉** | 芯棒沿 ±长轴平移 d 后与产品的干涉量 | d 分级取 `1 / 10 / 0.25L / 0.5L / 0.75L / L+5`：近处查局部倒扣，远处确认能整体抽出；**分正/负两个方向分别判定** |
| 逐面"内孔贴合率"（可选） | 产品贴芯面面积 ÷ 芯棒侧面面积 → 1.00 | `--check-contact`：要给芯棒做点分类，757 mm 管件要 **22 min**，默认关闭 |
| 精确布尔啃料（可选） | 芯棒 ∩ 产品 ≈ 0 | `--exact-overlap`：零间隙贴合时布尔极慢，757 mm 管件 **1308 s**，默认关闭 |

> 检查判据踩过的坑（都写进代码注释了，避免以后再犯）：
> 1. **别拿芯棒做点分类器**：芯棒面数可能上千（本例 20~1000+ 面），单次 `BRepClass3d_SolidClassifier.Perform`
>    约 0.6~2 s。早期版本用它逐点判 3862 个点 → 一项校验跑了 **45 min**。
> 2. **"芯棒表面点是否在产品材料里"不是有效的啃料判据**：芯棒填满内孔后，它侧面外面本来就是管壁，
>    零间隙下分类结果在 `ON/IN` 之间跳（实测误报 17%）。有效判据是**站位截面面积对比**或精确布尔。
> 3. **不能用面的形心做探针**：整圆柱面的"面积形心"在轴线上（不在面上），会把它误判成贴芯面
>    （实测贴合率虚高到 1.23）。
> 4. 逐面取探针不能按"等间隔下标"取，也不能只用最远点采样：`sample_surface` 是 u 外层/v 内层的参数网格，
>    简单取点会全落在参数同一端（圆柱面上就是同一个轴向站位，恰好在芯棒端面之外 → 误判该面没填实）。
> 5. 带孔的面（如管件环形端面）其参数矩形包含孔洞区域，采样点会落在孔里 —— 涉及"按面取点"的判据都要提防。

---

## 三、用法

### 命令行

```bash
# 独立运行（推荐先跑这个看日志）
venv\Scripts\python core_design.py <产品.stp> --output ./output --prefix core

# 或走主程序（--mode core）
venv\Scripts\python main.py --mode core --part <产品.stp> --output ./output

# 内置自测：Φ40×3 空心管，芯棒应为 Φ34×100 圆柱（体积 90792 mm³）
venv\Scripts\python core_design.py --selftest
```

| 参数 | 默认 | 说明 |
|------|------|------|
| `--axis auto\|x\|y` | auto | 内孔长轴方向（auto = 取水平长轴） |
| `--clearance` | 0 | 芯棒**单边装配间隙**（mm）。0 = 与内孔完全贴合；实际做芯棒建议 0.05~0.1 |
| `--plate-depth` | 1.0 | 端口封盖拉伸长度（mm），只影响布尔过程，不影响最终芯棒 |
| `--quick` | 关 | 抽芯检验只做 1/10 mm 两个距离（省 ~4 min，但不逐级扫描全程） |
| `--exact-overlap` | 关 | 加做精确布尔"芯棒 ∩ 产品"判啃料（+22 min 量级） |
| `--check-contact` | 关 | 加做逐面"内孔贴合率"（+22 min 量级） |
| `--no-verify` | 关 | 跳过贴合/抽芯检验（最快，只出几何，约 6~7 min） |

### Python API

```python
from core_design import build_core_pin, design_core

res = build_core_pin(product_shape, axis=None, clearance=0.05, verify=True)
res.core            # 芯棒实体
res.volume          # 体积
res.ports           # 两端端口信息（位置 / 孔口面积 / 是否开口 / 齐平偏差）
res.fill            # {"fill_ratio": 1.0, "overlap_volume": 0.0, ...}
res.pull            # {"plus": 最大干涉, "minus": ..., "removable": True/False, ...}
res.removable       # 能否整体直抽
res.warnings        # 校验提示列表

design_core(part_path="产品.stp", output_dir="./output", prefix="core")   # 含导出
```

### 输出文件

| 文件 | 内容 |
|------|------|
| `{prefix}_core_pin.step` | **芯棒**（单独一个实体） |
| `{prefix}_core_assembly.step` | **产品 + 芯棒**装配体（同一坐标系、按实际配合位置组合，可直接核对贴合） |
| `{prefix}_metadata.json` | 端口 / 截面轮廓 / 贴合 / 抽芯等全部检验数据 |

### Web 面板

左侧功能列表 **「🧩 模芯设计（芯棒）」**（顶栏也有入口）：
选长轴方向 → 填单边间隙 / 端口封盖长度 → 「🧩 生成芯棒」。
结果区给出芯棒尺寸体积、两端端口信息、贴合率、啃料抽检、抽芯方向结论，并提供
「⬇️ 芯棒 STEP」「⬇️ 产品+芯棒装配体」「👁 预览芯棒」「👁 看装配（半透明）」「🔍 剖视装配」。

两个勾选项：**快速校验**（抽芯只做 1/10 mm）、**精确布尔校验啃料**（慢，757 mm 管件约 +20 min）。

对应接口：`POST /api/core_generate`、`GET /api/core_download`（见 `web_box_generator/app.py`）；
输出目录 `web_box_generator/uploads/core_output/<task_id>/`。

---

## 四、实测结果（`24TK_1324-1_ROTATE_x_CCW_90deg.stp`，757 × 58 × 76 mm 薄壁管件）

```
产品体积            260 122.53 mm³
端口(负端) 端口面 X=0.000    封盖站位 X=0.019   开口 2101.5 mm²（平面端面，齐平）
端口(正端) 端口面 X=755.908  封盖站位 X=754.995 开口 3389.2 mm²（圆角收口，端面差 0.82 mm）
取芯结果            2 个实体（模具主体 2 596 108 + 内孔腔 1 617 383）
质量守恒            −119.6 mm³（−0.0028%）
芯棒                1 617 382.5 mm³，755.0 × 54.0 × 71.8 mm，轴向长 755.00 mm
内孔填满(站位截面)   21 个站位：芯棒截面相对内孔 −0.0001% ~ +0.017%（均值 +0.002%）
芯棒截面            2096.9 ~ 2549.0 mm²（最粗在 X=726，粗细比 1.22）
抽芯(正向 +X)       1/10/189/378/566/760 mm 干涉 0 / 0.27 / 0.23 / 0.39 / 0.82 / 0 mm³ ✅
抽芯(负向 -X)       1/10 mm 干涉 1290 / 7240 mm³ ❌（粗端过不去）
结论                **可从 +X 端（端口大的一端）整体直抽**（判定阈值 323.5 mm³）
警告                无
```

抽芯结论是分方向判定的：本件内孔从负端到正端**逐渐变粗、中心上移**，
所以芯棒只能从**粗的那一端（+X）**整体抽出，从细端拔必然刮伤内壁。

导出结果是干净的：`core_core_pin.step` = **1 个实体**（1 617 374.6 mm³，与计算值一致），
`core_core_assembly.step` = **2 个实体**（产品 260 122.5 + 芯棒）。

**耗时**（同一台机器，默认校验）≈ **12~15 min**：

| 阶段 | 耗时 | 备注 |
|---|---|---|
| 端口定位（二分找管壁闭合截面） | 19 s | 每次截面 ~0.9 s × 约 20 次 |
| **取芯块 − 产品 − 封盖 布尔** | **315~390 s** | 4.5×10⁶ mm³ 块被 757 mm、52 面的薄壳切；OCC 单线程面-面求交，绕不开 |
| 站位截面 + 质量守恒 | ~40 s | 21 站 × 2 个截面 |
| **抽芯检验（6 距离 × 2 方向 = 12 次布尔求交）** | **~330~390 s** | 每次 ~27 s |
| 写 2 个 STEP（5.7 万实体）+ 读模型 + 采样 | ~70 s | |

想更快：`--quick`（抽芯只做 1/10 mm，省 ~4 min）、`--no-verify`（只出几何，约 6~7 min）、
`--no-verify` + `--quick` 组合最快。反方向的 `--exact-overlap` / `--check-contact` 会更慢（+22 min 量级）。

> **只想拿"上下模 + 芯棒 + 四件套"的话，不需要跑本模块**：`main.py --mold-type pipe` 挖包络时
> 就把芯棒一起切出来了（白捡），`--no-verify --parallel` 实测 **452 s（7.5 min）** 出全套
> （三件套之前要 48 min）。本模块只在需要**单独出芯棒图**（端面严格齐平 / 留装配间隙 / 逐站位校验）时才跑。

---

## 五、已知限制与后续可做

1. **能不能直抽要看产品形状**：本例内孔"逐渐变粗 + 中心上移"，结论是**只能从粗端（+X）整体直抽**；
   程序按 ±两个方向分别判定（`pull_check.removable_plus / removable_minus`）并给出方向。
   若两个方向都不行，会提示改用低熔点合金/溶芯、芯棒分段 + 斜抽机构，或把端口做镶件。
2. **端面与端口齐平**：封盖的里面那一端必须落在**管壁仍闭合**的截面上（否则内孔腔会从
   "管壁已结束、封盖还没开始"的那一小段径向漏出去，切不断、取不出芯棒 —— 实测正端圆角
   收口处漏 0.9 mm 时就是这个现象）。所以端口是圆角/斜切收口时，芯棒端面会落在
   "最后一个闭合截面"处，与最外点差 ≤0.9 mm，元数据里 `ports[].flush_gap_mm` 会写明。
3. **侧面开孔**：产品侧壁有孔时内孔腔与外界连通，模块会尝试用"向外拉伸"的薄片封住孔口
   （封住后芯棒在该处会有凸起 → 妨碍直抽，会给出警告），失败会退回仅端口封盖并提示。
4. 尚未做的工艺特征：芯棒**夹持柄/工艺段**、脱模斜度、表面粗糙度、与上下模一起的四件套装配体导出。
