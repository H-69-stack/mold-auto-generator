# 管件硬模成型模具生成（mold_type = "pipe"）

> 针对**薄壁中空管件**（如 `24TK_1324-1`：D 形截面、壁厚 2 mm、长 757 mm、
> 末端上翘的弯管）的模具生成逻辑与问题定位记录。

> 相关模块：**模芯设计（芯棒 / 内芯）** 见 [`CORE_DESIGN_README.md`](CORE_DESIGN_README.md) ——
> 模具管的是产品**外形**（内孔被填实成包络），芯棒管的是产品**内孔**（沿两端端口面填满）。
> **管件模式默认就在同一次布尔里把芯棒一起切出来**，直接给出四件套装配体；
> 要"端面严格齐平 / 带装配间隙 / 逐站位校验"的正式芯棒图时，用 `main.py --mode core`。

---

## 一、用户描述的管件模具逻辑

> 直接把管件放在一个实体长方体的**中心**，挖出一个**与产品所占空间的外形一样大的空腔**，
> 然后再进行分模。

本程序按此实现为 `mold_type="pipe"`（`main.py::_generate_pipe_mold` + `pipe_mold.py`）：

1. **产品居中**：模芯 = 产品包围盒 + 双边余量，产品放在模芯正中心（XY + Z 全部居中）；
2. **外形包络型腔**：型腔 = 产品**外形**（内孔被填实），模具只成形管件外表面；
3. **分模面**（`--parting-surface` / Web 面板可选）：
   * `plane`（**默认**）——**水平面**，模具最好加工；高度由程序自动取安全值；
   * `silhouette` —— 沿产品侧影线随形，管件弯曲/上翘大时更贴合；
4. **装配体**（额外导出，都可直接打开核对配合）：
   * **四件套**「上模 + 产品 + 芯棒 + 下模」`{prefix}_mold4_assembly.step`；
   * 三件套「上模 + 产品 + 下模」`{prefix}_mold_assembly.step`；
   * 模芯装配体「产品 + 型腔模」`{prefix}_core_assembly.step`；
   * 芯棒单件 `{prefix}_core_pin.step`。
5. **闭环校验**：分模后做"上模抬起 × 产品""产品顶出 × 下模""上模 × 下模"干涉检验（`--no-verify` 可跳过，省 ~5 min）。

> **芯棒是白捡的**：挖"外形包络"型腔时，内孔里那块"内芯条"本来就要被切断剔除 ——
> 它就是产品的**内孔腔 = 芯棒**。所以四件套里那块芯棒**不额外花布尔时间**
> （以前要单独跑一次 `--mode core`，多花 ~5 min 的布尔）。芯棒与上下模是同一次布尔
> 切出来的互补两块，**天然互不重叠**，不需要再做干涉检查。
> 需要"端面与端口严格齐平 / 带装配间隙 / 逐站位校验"的正式芯棒时，再用
> [`CORE_DESIGN_README.md`](CORE_DESIGN_README.md) 里那套独立模块出图。

用法：

```bash
# 命令行（默认水平分模面；默认一起出四件套；默认做干涉校验）
venv\Scripts\python main.py --part 24TK_1324-1_ROTATE_x_CCW_90deg.stp --mold-type pipe --output ./output
# 只要几何 + 装配体、不要干涉校验（快 ~5 min）
venv\Scripts\python main.py --part <产品.stp> --mold-type pipe --no-verify
# 布尔开多核并行（可能更快，若结果异常去掉即可）
venv\Scripts\python main.py --part <产品.stp> --mold-type pipe --parallel
# 不需要芯棒/四件套
venv\Scripts\python main.py --part <产品.stp> --mold-type pipe --no-core
# Web：上传模型 → 面板「🤖 智能模具」→ 模具工艺类型选「管件硬模成型」
#      勾选项：同时出芯棒+四件套 / 跳过干涉校验 / 布尔多核并行
```

### 水平分模面高度怎么定（`pipe_mold.choose_plane_height`）

水平面不能随便取，程序用"贴模接触点"（模具真正成形到的产品表面，内孔面不算）算三个量：

| 量 | 含义 | 作用 |
|----|------|------|
| `z_hi` = 朝上接触面最低 z | 产品最靠下的"朝上成形面" | 分模面必须 **低于**它，否则下模在该面之上多留一层料，产品顶出被挡 |
| `z_lo` = 逐列(5mm 网格)最低材料的最大 z | 产品"底面包围"的最高处 | 分模面必须 **高于**它，否则上模在产品下方多留一层料（台阶） |
| `z_sil` = 侧影曲线最低点 | 产品最窄侧的侧影高度 | 分型线的自然位置 |

取 `z_p = min(z_sil − 0.2, z_hi)`，并保证 `≥ z_lo + 0.2`。
本件结果：**z_p = 43.08 mm**（侧影最低 43.28、朝上接触面最低 50.56、逐列最低材料最大 29.54）。

若 `z_lo > z_hi`（存在斜切端面之类的真实倒扣），程序会打印提示，最终以开模/顶出干涉检验结果为准。

### 自己指定分模面高度（2026-09 新增）

**模芯总高不变，只把分模面上下移动** —— 上模高 + 下模高 恒等于模芯高。

```bash
# 命令行：--parting-z <高度>
venv\Scripts\python main.py --part <产品.stp> --mold-type pipe --parting-z 60 --no-verify
# Web：面板「🤖 智能模具」→ 分模面形式选 plane → 分模面高度选「自定义」→ 输数值
#      点「📐 取推荐值」可先算出自动推荐高度 + 安全区间（不跑布尔，几十秒）
```

程序回报（日志 / 元数据 / Web 结果区）：

```
- 沿水平分模面 z=60.00 切分上/下模（用户指定；自动推荐 79.30，安全区间 79.30 ~ 49.48）...
  [提示] 指定的分模面 z=60.00 mm 超出自动推荐的安全区间 [79.30, 49.48]：可能在上/下模留下台阶或钩料，以开模/顶出干涉检验为准
- 上/下模高度: 下模 60.00 mm（z 0.00 ~ 60.00）+ 上模 46.37 mm（z 60.00 ~ 106.37）= 模芯高 106.37 mm
  （实测包围盒: 上模 z∈[60.00, 106.37] ｜ 下模 z∈[-0.00, 60.00]）
```

元数据里对应字段：`parting_z_mm`、`parting_z_source`（auto/user）、`parting_z_auto_mm`、
`parting_z_safe_range_mm`、`upper_mold_height_mm`、`lower_mold_height_mm`、
`lower_mold_height_mm`+`upper_mold_height_mm = 模芯高`。

### 「自动推荐高度」怎么快速拿到（`/api/pipe_plane_suggest`）

排样分析（采样 → 成形面判定 → 侧影 → 安全值）**不需要先跑 4~5 分钟的挖腔布尔**：
`pipe_mold.outer_contact_flags` 用**垂直射线求交**（`IntCurvesFace_ShapeIntersector`）区分
"内孔面 / 外形面" —— 朝上的面若沿 +Z 还能打到产品材料，说明它在腔体里（上方压着一层壁），
就不是成形面。实测与"用型腔模判定"的结果关键量一致（成形面 37/52、朝上最低 z 都是 50.56），
大件几十秒出结果。CLI 里也可以用 `python -c "from main import suggest_pipe_plane; ..."` 调。

> 注意：`z_lo > z_hi`（**没有可行的水平面区间**）时，程序会明确告诉你"这个件单一水平面必然
> 留台阶或钩料"，此时可选：① 接受局部台阶并用开模/顶出干涉校验确认能不能用；
> ② 换 `--parting-surface silhouette`（沿侧影随形）。实测 `24TK_1324-31`（强弯曲管件）
> 就是这种情况：逐列最低材料 79.10 > 朝上成形面最低 49.48。

---

## 一之二、四件套装配体 & 速度（2026-09 新增）

### 四件套「上模 + 产品 + 芯棒 + 下模」

`main.build_mold4_assembly()` 把 **[上模, 产品, 芯棒, 下模]** 按合模状态放进一个 compound，
导出 `{prefix}_mold4_assembly.step`（4 个实体，同一坐标系）。

**芯棒是白捡的**：`build_envelope_die` 挖"外形包络"时，内孔里那块"内芯条"本来就要被切断剔除 ——
它就是产品的**内孔腔 = 芯棒**。以前要单独跑一次 `main.py --mode core`（多花约 5 min 的布尔），
现在直接把它留下即可（`keep_pieces=True`），**不额外花任何布尔时间**。
四件套里的四块是同一次布尔切出来的互补实体，**天然互不重叠、配合正确**，不需要再做干涉检查。

实测（本件，`_pipe_diag/mold4_run`）：

| 结构体 | 体积 mm³ | 包围盒 z |
|---|---|---|
| ① 上模 | 4 637 173.8 | 28.71 … 116.01 |
| ② 产品 | 260 122.5 | 20.00 … 96.00 |
| ③ 芯棒（内孔腔） | 1 616 051.6 | 21.99 … 94.22 |
| ④ 下模 | 2 560 688.5 | 0.00 … 67.49 |

模芯 9 087 005.8 − (①+②+③+④) = 12 958 mm³，正好是封盖切掉的那点料 + 布尔容差
→ 四块互补、无重叠 ✅。另单独导出 `{prefix}_core_pin.step`（芯棒单件）。

> 与 [`CORE_DESIGN_README.md`](CORE_DESIGN_README.md) 那套独立芯棒模块的差别：
> 四件套里的芯棒端面落在**封盖所在站位**（比端口面短 0.1~0.5 mm），体积 1 616 064.7 mm³；
> 独立模块用二分法把站位推到端口面（754.995 / 0.019），体积 1 617 382.5 mm³（差 0.08%）。
> 要"严格齐平 / 留装配间隙 / 逐站位校验"的正式芯棒图，用 `--mode core`。

### 速度

| 流程 | 耗时 | 说明 |
|---|---|---|
| 旧：管件模具（含干涉校验）+ 单独跑一次芯棒 | 748 s + 2120 s ≈ **48 min** | 两次大布尔、两套校验 |
| 新：一次跑出上下模 + 芯棒 + 四件套（`--no-verify --parallel`） | **452 s（7.5 min）** | 布尔只跑一次；跳过干涉校验 |
| 新：同上但保留干涉校验 | ≈ 13 min | `verify_demold` 6 次布尔约 5 min |

耗时分解（本次）：`读模型+采样 0.9 s ｜ 外形包络布尔 267.5 s ｜ 侧影 53.8 s ｜ 分模 125.4 s ｜ 导出 7 个 STEP ~60 s`。
大头只有两个：**一次大布尔（4.5 min）+ 写 STEP**，其余都是秒级。

### 2026-09 二次优化：复杂件 18 min → 3 min（6×）

`24TK_2052-2`（488 mm、95 个面）实测 **1103.6 s → 185.4 s**，输出与体积结果**完全一致**
（上模 785 830.8 / 下模 4 155 969.2 / 芯棒 91 765.5 mm³、分模面 z=128.86 都一样）。两处改动：

| # | 改动 | 效果 |
|---|------|------|
| 1 | **贴模成形面判定改用射线法**：`outer_contact_flags` 用 `IntCurvesFace_ShapeIntersector` 从面上一点沿 ±Z 打射线，打到产品材料 → 该面在腔体里（内孔面），不是成形面 | 原来对上千面的型腔模逐面做 `BRepClass3d_SolidClassifier`：**462 s → 0.2 s**；关键量"朝上成形面最低 z"与布尔法一致（757 mm 件都是 50.56）。射线法退化（成形点 <5%）时才退回布尔法 |
| 2 | **默认只导出 4 个文件**（上模 / 下模 / 芯棒 / 四件套 + 元数据），不再多写"型腔模 / 模芯装配体 / 三件套"三份大 STEP | 导出省 1~4 min、输出目录干净；需要时 `--all-outputs` 恢复 |

优化后分解：`采样 1.6 s ｜ 外形包络布尔 147 s（含一次封盖重试）｜ 侧影 0.2 s ｜ 分模 34.8 s ｜ 导出 ~30 s`。

### 2026-09 三次优化：改为「截断块法」——封盖布尔卡 20 min → 整条管线 20 s

`24TK_1324-2`（765.7 mm，端口带喇叭口，62 个面）实测：**封盖法卡在"切断内芯条"这一步
20 min 以上没有结果**；而一旦封盖没封住，型腔就没挖出内孔 —— 症状是芯棒只剩 **1 485.5 mm³**
（真实内孔腔 **2 066 930 mm³**）、上模裂成 2 个分离实体、自动分模高度算到不可能的区间
（`安全区间 [131.33, 64.39]`，上下限颠倒）。新方法整条管线 **20 s**：

| 指标 | 旧（封盖法） | 新（截断块法） |
|---|---|---|
| 芯棒（内孔腔） | 1 485.5 mm³（错） | **2 066 930.1 mm³** |
| 上模 / 下模实体数 | 上模 2 块（错） | 各 **1 块** |
| 上模 / 下模体积 | — | 6 243 741.9 / 6 206 872.6 mm³ |
| 自动分模面 z | 131.33（安全区间不可行） | **50.48**（安全区间 47.09 ~ 55.55，可行） |
| 上下模高度 | — | 下模 50.48 + 上模 50.88 = 模芯高 101.36（总高不变） |
| 用时 | 卡 20 min 以上 | **20 s**（含导出） |

**原理（`pipe_mold.trim_core_and_hull`）**：把模芯块沿长轴**截到产品两端端口平面**后再做
`截断块 − 产品` —— 内孔里的"内芯条"径向被管壁隔开、轴向被截断平面切断，与主体不再相连，
于是**一次普通布尔就自动分离出芯棒**；再把 `产品 + 芯棒` 融合成**外形包络**（内孔填实），
用整块模芯减包络，就得到**带两端封料壁**的正确型腔。全程没有封盖薄片。

**为什么封盖法会卡**：封盖是"孔口轮廓向外偏置 0.4 mm"的棱柱，侧面与管内壁**近似平行（相切）**，
OCC 在近重合面上会爆炸式变慢；想靠"把封盖加大到穿出管壁"绕开相切也不行 —— 本件壁厚只有
~1.6 mm，封盖一大就把模具端面的封料环一起吃掉了。截断块法只有普通**横切**布尔，实测 1~2 s。

**三个必须踩对的坑**：

| 坑 | 现象 | 正确做法 |
|---|---|---|
| 截断平面必须落在**管壁还存在**的位置 | 用表面采样极值当端面：面的 (u,v) 参数矩形包含**被裁掉**的区域，采样点会落到真实几何之外（`24TK_1324-2` 虚高 **0.15 / 0.53 mm**，`24TK_1324-1` +X 端虚高 **0.92 mm**）。切在管壁端面之外 → 端口那圈没有管壁隔着，内芯条仍与主体连通 → 内孔腔切不出来（正是"分模面不对"的病根） | `wall_end_station()`：用**截面环数二分**量出管壁真正的端面（管件端口常有倒棱/倒圆小面，采样极值不是端面） |
| 截断面正好压在管壁端面上 = **相切**配置 | 布尔能卡几分钟到几十分钟 | 截面往里让 **0.2 mm**（普通横切，秒级）。让 0.02~0.05 mm 又会在模具端面留下又细又长的"薄刃面"，0.2 mm 的过渡面才是干净的小面 |
| 封盖/塞子侧面**不要外扩** | 外扩 0.3 mm → 侧面与管内壁近乎平行（相切）→ 一次截断 **139 s**；而且偏置后的面容易自交，做出来的薄片还会把模具端面啃出深坑 | 直接用精确内孔口环（侧面与内壁是"面-面重合"，BOPAlgo 走 same-domain 通道，0.1 s 且干净）。这一条也是 `24TK_1324-2` 原先卡 20 min 的根因 |
**兜底链**：管壁端面截断 → 端口盖片（`port_cap_slabs`，用端口面取口环）→ 端口平面截断（`port_cutters`）→ 端口塞子（`port_corks`）→ 原来的"孔口封盖薄片"法（`hole_sealing_plates`，含加厚封盖重试）。任何一环拿不到内芯条都会在日志里写明原因，不会静默出一个错模具。

**两个管件的最终实测**：

| 指标 | `24TK_1324-2`（765 mm 直管+喇叭口） | `24TK_1324-1`（757 mm 上翘弯管，+X 端收口倒圆 0.92 mm） |
|---|---|---|
| 芯棒 | 2 066 396.3 mm³ | 1 616 368.8 mm³（= 内孔 2101.7 mm² × 755.5 mm，与老版 1 616 064.7 一致） |
| 上模 / 下模 | 6 243 902.5 / 6 206 944.7 mm³，各 1 个实体 | 4 709 998.1 / 2 500 680.8 mm³，各 1 个实体 |
| 自动分模面 z | 50.48（安全区间 47.09 ~ 55.55） | 41.35（安全区间 29.74 ~ 50.56） |
| 用时 | **36 s** | **25 s**（旧封盖法 **1021.9 s = 17 min**，还带尖角/薄刃） |

### 2026-09 四次优化：型腔台阶 / 边缘不贴弧度 / 盲端与侧向开口（用户反馈 2、3、4）

| 现象 | 根因 | 修复 |
|---|---|---|
| 上下模型腔左右各有一小块**台阶凸起** | 截断时截面往里让了 0.2 mm（不让位就是相切、布尔很慢），芯棒比真实内孔腔短 0.2 mm → `外形包络 = 产品 + 芯棒` 少了端口两片小圆片 → 模具在端口多出一圈台阶 | `_extend_core_to_wall_ends()`：在管壁端面往里 0.25 mm 取内孔环、向外拉 0.3 mm 补上再融合，芯棒端面与端口齐平 |
| 型腔边缘不是光滑圆弧，是"两个片面成的一个角"，不贴合产品弧度 | ① 该件的**水平分模面无解**（安全区间上下限颠倒：斜切端面 / 局部倒扣），程序却硬按"下模约束"把面抬到 `column_low_max` 以上 → 分模面**切进产品成形面**；② 侧影随形分模面是**折线**扫出来的，本身就是一个个平面小面 | ① 检测到安全区间颠倒时**自动改用侧影随形**并在日志/metadata 里写明；② `_profile_prism(..., spline=True)`：剖面改用**插值样条**（`GeomAPI_Interpolate` + C2 连续），分模面成为光滑曲面 |
| `24TK_2052-2`：生成很久、型腔和芯棒都不对 | 该件**+X 端是收口盲端**（截面环数从 2 → 1 → 0），且**管壁上有侧向开口**（x ≈ +65.6 / +91.4，从内孔中心打射线只穿透 1 次）；内孔腔从侧洞里与主体相连 → **两端截断原理上切不断**，于是掉进最慢的兜底路径 | ① 截断改成**允许单端**（盲端不截）；② 撤掉"端口塞子"这个慢兜底（它侧面与内壁近乎重合，实测卡 10 min+）；③ 新增 `detect_wall_openings()`：扫出侧向开口并**明确告知需要侧向抽芯**，不再静默产出错模具。实测 2052-2：卡 10 min+ → **171.8 s** 跑完，芯棒 93 076.1 mm³（老版 91 765.5） |

**关于侧向开口的说明（重要）**：管壁上有侧向开口（三通口、侧窗、腰形孔）时，内孔腔
从洞里与模具主体相连，**两端截断/封盖在原理上无法分离** —— 这种结构在真实模具里需要
**侧向抽芯（斜顶 / 滑块）**。本程序目前不生成侧抽芯，只会给出明确警告，并把该件按
"侧向开口存在"如实标注在 metadata 的 `mold_warnings` 里。

### 2026-09 五次优化：分模面回到"水平面" + 导出坐标系对齐（用户反馈 1、2、3、4）

| 用户反馈 | 之前的问题 | 现在的做法 |
|---|---|---|
| **1. 要有水平分模面，高度我自己填** | 上一版在"安全区间上下限颠倒"时**自动切到侧影随形**，把用户要的水平面弄没了 | **取消自动切换**：只要选「水平面」就一定是水平面（自动高度 / 自己填的高度都直接水平切开）；区间颠倒时只给提示，并报出本件侧影高度范围（本件 35.45 ~ 50.47）方便填值 |
| **2. 型腔底部两块片拼起来、侧面那个面是尖角（不贴合）** | 侧影随形分模面是**折线**扫出来的（本件 62 个站位 = 62 条平面小面），而且该件侧影高度从 50.5 陡降到 38.3 → 型腔边缘出现"两片面成角" | ① 回到水平面（一个平面，不再有拼接小面）；② 侧影随形保留时，剖面先**重采样到 ≤24 点再插值成样条**（`GeomAPI_Interpolate`），不再是折线 |
| **3. 管件和上下模实体"重叠"** | 模具是在**模芯坐标系**（产品被平移居中）里算并导出的；用户拿**原始坐标**的产品一比，产品就落在模芯实体里，看着像"重叠" | 导出前把上模 / 下模 / 芯棒 / 四件套**整体平移回输入数模的原始坐标系**（`_shift_shape(..., -align_offset)`）。实测：原产品 X −51.1~611.4，导出上模 X −72.8~633.1（= 产品 ± 单边余量），直接装配能对上 |
| **4. 型腔端口的小台阶还在** | 与 2 同源（拼接分模面在端口处跳变） | 回到水平面后分模面是一个平面；另把芯棒端面补齐到管壁端面（`_extend_core_to_wall_ends`），端口不再多出小台阶 |

**关于"水平面 + 斜切端面"的取舍（如实说明）**：像 `24TK_1324-31` 这种端面斜切 / 局部
倒扣的件，**任何单一水平面**都会在部分站位留下台阶或钩料（程序会提示，并给出本件侧影
高度范围供参考）。这是几何决定的，不是算法 bug：要完全随形就得用「侧影随形」分模面
（曲面，加工麻烦一些）。

### 2026-09 六次优化：**端口内孔被模料堵住**（真正的"端口台阶 / 尖角 / 看着像重叠"）

前几轮一直把"端口小台阶"归到分模面上，其实**根因在芯棒（内孔腔）长度**上。这轮用
`_pipe_diag/` 下的一组探针把它彻底定位了：

| 现象 | 实测证据 | 根因 |
|---|---|---|
| 端口内孔里有模料"塞子"，上/下模在端口处对咬成尖角；把管件与上下模装在一起看像"模具插进管件、和管件重叠" | 在内孔中心线上按站位做点分类：`x = −26.3 ~ −25.2` 与 `583.6 ~ 587` 处，内孔截面 **92~100%** 的点落在上/下模实体里，芯棒覆盖率 **0%** | 芯棒太短：截断面被放在"管壁端面里侧 0.2 mm"，而端口端面是**斜切**的（本件 −X 端斜 0.87 mm、+X 端斜 2.67 mm）→ 芯棒比真实内孔短 0.5~2.9 mm， molds 就在那一段把内孔填死了 |
| "端口小台阶凸起还是存在"（上一轮说已修，实际没修） | `_extend_core_to_wall_ends()` 里写的是 `bool_fuse(core, discs, …)`，而 `bool_fuse` **只接两个 TopoDS_Shape**（传 list 直接 `TypeError`），异常又被 `except: pass` 吞掉 → **这个"补齐"从写下那天起就从没生效过** | 两个 bug 叠在一起：① 截断位置切在斜切端口内侧；② 补齐那一步静默失效 |

**修复**

1. **截断改用「端口平面截断」为主路径**（`port_cut_planes` + `port_cutters`）：
   沿产品**端口端面所在平面**裁模芯块，芯棒长度正好 = 内孔全长，端口内孔不再留料。
   这一刀是"箱体 − 大棱柱"的**平面**布尔，实测 **0.01 s**；随后"减产品"1.4 s ——
   跟原来的垂直截断同级，甚至更快（原来把它排在兜底第 3 位，所以从来没被用到）。
   垂直长轴截断（截面环二分 + 让位 0.2 mm）降为**备选 1**。
   注：让位量必须是 0 或 ≥0.2 mm —— 实测 0.02 mm 时两个实体被布尔容差"焊"在一起
   （内孔腔切不出来），而**正好 0**（贴着产品端面）反而是干净的两块。
2. `_extend_core_to_wall_ends()` 修掉 list 传参：补片先合成 `TopoDS_Compound` 再
   `bool_fuse(core, patch)`；并且只在退回"垂直截断"时才调用（走端口平面时芯棒本来就是全长）。
   函数里补片的轴向长度也改成"一直拉到真实端面（表面采样极值）之外 0.2 mm"，并用端口平面削平。

**实测（`24TK_1324-31_ROTATE_x_CW_90deg_ROTATE_y_CW_4deg`，水平分模面 z=40，`--no-verify`）**

| 指标 | 修复前 | 修复后 |
|---|---|---|
| 截断路径 | 管壁端面截断（垂直，让位 0.2） | **端口平面截断** |
| 芯棒体积 | 567 216.3 mm³（端口缺料） | **569 388.9 mm³**（= 内孔全长） |
| 外形包络 | 688 519.9 mm³ | **690 692.6 mm³**（= 产品 121 304.5 + 芯棒 569 388.9，误差 0.8 mm³） |
| **端口内孔里的模料** | **约 2 700 mm³（塞子）** | **0.000 mm³** |
| 上模 × 产品 / 下模 × 产品 / 芯棒 × 产品 / 上模 × 下模 | 0 / 0 / 0 / 0 | **0.0000 / 0.0000 / 0.0000 / 0.0000 mm³** |
| 整条管线耗时 | 103.8 s | **64.4 s**（快了 ~40%） |

验证脚本（都在 `_pipe_diag/`，可重复跑）：
`verify_port_gap.py`（用端口平面裁出"内孔该是空的"那块，去和上下模求交 → 0）、
`check_plug2.py`（沿长轴逐站位把内孔里的点丢给上下模/芯棒做 `BRepClass3d` 判定）、
`verify_attached.py`（产品 × 上/下模/芯棒的体积求交）、
`verify_cavity.py`（反算型腔 = 模芯 − 上下模，和"产品 ∪ 芯棒"对比）。

### 已知但**本轮未改**的一项：产品包围盒虚胖（BSpline 控制点）

`geometry_utils.shape_bbox`（`Bnd_Box`）对 BSpline 面会把包围盒撑大。本件实测：

| | X 范围 | Y | Z |
|---|---|---|---|
| `shape_bbox` 报告 | −51.09 ~ 611.35（长 662.4） | −4.83 ~ 45.51 | −19.87 ~ 31.79 |
| 真实几何（网格顶点 + 平面端面） | **−26.10 ~ 586.41（长 612.5）** | −4.17 ~ 44.84 | −19.40 ~ 30.02 |

影响：模芯块在长轴方向**多出 2 × 25 mm**（702.4 而不是 652.5，多约 7% 材料），
metadata 里 `product_size_mm` / `product_bbox` 报的也是虚胖值；产品居中因此 Z 向偏 0.65 mm。
**型腔贴合、分模、芯棒都不受影响**（那些都用表面采样点算，不用包围盒），所以没动它 ——
要改就得同时改模芯尺寸/居中/上报口径，会让"分模面高度"这个用户填的数值基准整体平移，
需要单独一轮做（改之前先和用户确认口径）。

---

## 二、原程序在管件上“分模出问题”的四个根因（已定位并修复）

### 1. 型腔直接挖产品实体 → 管件内孔里留下"内芯条"，产品被卡死

`cavity_cutter.cut_cavity` 做的是 `模芯 - 产品实体`。管件是**中空**的，内孔贯通，
于是内孔里那一坨模料会**从两端与模芯主体相连**，形成一根 1.6×10⁶ mm³ 的"内芯条"。

实测（`_pipe_diag/analyze_mold.py`、`run_case.py`）：

| 方案 | 上模抬起×产品 | 产品顶出×下模 |
|------|--------------|--------------|
| 现有管线 `contour` | 0（上模只剩那根内芯） | **卡死** |
| 现有管线 `cavity_bottom` | 0 | **24 819 ~ 67 420 mm³ 干涉** |

上模也会被内芯"钩住"（干涉 9.2×10⁵~1.0×10⁶ mm³），即**模开不开、件顶不出**。

**修复**：型腔改为**产品外形包络**——先用"端面截面孔轮廓封盖薄片"把内芯条与主体切断，
再只保留主体（`pipe_mold.build_envelope_die`）。管件内孔不再参与成形
（这正是硬模成型/内高压成形的做法：模具只做外形，内孔由管坯自身或内压/芯棒保证）。

### 2. 一刀水平分模面 → 管件末端上翘段形成台阶/倒扣

开模方向 +Z 时，分型线必须落在产品表面**法向水平**处（侧影线，nz≈0）：
朝上的面必须归上模、朝下的面必须归下模。管件末端上翘，侧影线随之抬升，
用一个固定高度的水平面切，抬升段就会出现"料钩在产品下方"的台阶，
上模抬不起来（这正是实测到的 9.2×10⁵ mm³ 干涉）。

**修复**：`pipe_mold.silhouette_profile` 从**精确 BRep 表面采样**求每个站位的侧影高度
h(x)（本件 41.55 → 43.28 mm，随上翘变化 1.7 mm），分模面做成沿长轴的随形棱柱。
本件末端翘起段的侧影高度比直段高约 1.7 mm，正是旧算法"切不平"的地方。

### 3. 自动分模高度判据用"截面材料面积" → 薄壁件会选到切线高度（重大 bug）

`parting_splitter.find_max_projection_z` 原本比较的是截面**材料面积**。
对**薄壁中空件**，材料面积在"平面与曲面相切"的高度最大（切得又长又宽），
于是自动分模高度被选到产品**顶端/底端的切点**上。

实测本件：选出的自动分模高度 z=60.667（对齐后），而管件在该处**已经超过管子顶面**
（直段顶面在 59.15），等于把分模面放到了管子外面 —— 分模结果自然不合理。

**修复**：判据改为截面点云的**凸包面积**（= 真正的"投影轮廓面积"），
并在面积平台段取**中位高度**（避免贴到最底/最顶）。
内置测试件验证：自动分模高度由 20.0（贴底切点）变为 25.21（等宽段中间）。

### 4. 倒扣检测判据写反 → 把正常型腔底面全部误报为倒扣

`detect_undercuts` 原本是"法向朝下 且 低于分模面 → 倒扣"，这恰恰是**下模应该成形**
的正常面（产品底面）。所以管件一次性报了 **20 个**"倒扣候选"，真正的倒扣反而漏检。

**修复**：改为
* 朝上（nz>0）却位于分模面**以下** → 产品顶出会被挡（倒扣）；
* 朝下（nz<0）却位于分模面**以上** → 上模抬起会被钩住（倒扣）；
* 与分模面共面的面给 1e-6 容差，不再误报。

---

## 三、管线级修正后的实测结果（同一件、同一模芯）

模芯 797.5 × 98.2 × 116.0 mm，产品体积 260 122 mm³（**默认水平分模面**）：

| 指标 | 修复前（现有管线） | 修复后（pipe 模式，水平面） |
|------|-------------------|--------------------|
| 型腔模体积 | 8 826 866 mm³（含内芯条） | **7 197 825 mm³**（外形包络） |
| 内芯条 | 与主体相连、无法去除 | 切断剔除 1 616 065 mm³ |
| 上模抬起 × 产品 | 9.2×10⁵ ~ 1.0×10⁶ mm³ ❌ | **0.00 ~ 0.08 mm³ ✅** |
| 产品顶出 × 下模 | 2.5×10⁴ ~ 6.7×10⁴ mm³ ❌ | **0.00 ~ 2.12 mm³ ✅** |
| 上模 × 下模 | 0 | 0.00 mm³ ✅ |
| 上模/下模实体数 | — | 各 1 个 ✅（无掉块/浮块） |
| 分模面 | 一刀水平面（高度取错）+ 内芯卡死 | **水平面 z = 43.08 mm**（自动取安全高度） |
| 三件套装配体 | 无 | 上模 4 637 174 + 产品 260 123 + 下模 2 560 690 mm³，一个 STEP ✅ |
| 耗时 | 55~130 s | 约 550~750 s（布尔运算占绝大部分） |

> 残余的几 mm³ 干涉是**零间隙贴合下的布尔数值碎屑**（相对型腔模体积 < 0.0001%），
> 不是真实倒扣：同一几何重复测量会在 0 ~ 百 mm³ 之间波动。
> 程序据此把报警阈值设为 `max(2 mm³, 型腔模体积的 0.02%)`，同时把原始数值
> 全部打印并写进元数据 `demold_check`，供人工判断。

对比：若改用 `--parting-surface silhouette`（侧影随形面，h(x)=43.3…56.8 mm），
干涉量同样在 0 ~ 百 mm³ 量级，但分模面是曲面、加工麻烦 —— 所以默认给水平面。

内置测试件（长方体 + 凸台，实心无孔）回归验证：
水平面 z=24.80 mm（自动取在凸台台面之下），三个方向干涉全部 0.00 mm³。

---

## 四、实现要点与 OCCT 7.8 的坑

1. **`BRepPrimAPI_MakePrism` 不接受 solid**（抛 `Standard_NoSuchObject: Solids are not
   Processed`），只接受 face/shell/wire/edge。要"把一个实体往下扫掠成阴影体"必须自己拼。
2. **面拉伸（`MakePrism(face, vec)`）在本模型上不可靠**：圆柱/BSpline 面拉伸出的棱柱
   体积与"面积×距离"对不上（比值 0.10~0.98），部分结果 `BRepCheck` 无效，
   导致多棱柱求并/求差布尔大面积失败（`BOP 未完成`）。
   → 因此**没有**采用"阴影体 = 各面棱柱求并"的路线，改用"封盖切断内芯条 + 只保留主体"，
   全程只用可靠的"实体 - 实体"布尔。
3. **`shape_bbox` 会被 BSpline 控制点撑大**：本件 X 向包围盒 757.49，实际几何到
   756.3 附近就结束了（末端截面在 x>376.3 处为空）。所以端面封盖的取截面站位
   **必须自适应向内搜索**（`insets=(0.5, 0.9, 1.4, 2.0, 2.6, 3.4, 4.5, 6.0)`），
   并把孔轮廓**向外偏置 0.4 mm**，保证完整覆盖孔口、可靠切断内芯条。
4. **零面积面**：本件有 2 个面积为 0 的平面片，参与布尔会带来病态几何，需要过滤。
5. **零间隙贴合**：模具型腔面与产品表面完全重合（无间隙），布尔求交时直接用
   "平移后的实体 vs 实体"可以避开共面退化；点采样法在这种零间隙场合会因网格弦差
   产生假干涉（实测假报 2.5×10⁵ mm³），**不要**作为主判据。
6. **性能**：757 mm 那件（52 个面，含大量 BSpline）一次完整生成约 8~13 分钟，
   其中布尔运算占绝大部分；`24TK_1324-31`（661 mm、同样 2 处孔口）只要 **21 秒**。
7. **⚠️ 曲面上的孔口封盖不要用 `MakeFace(内环) + Prism`**（2026-09 实测踩到）：
   `24TK_1324-31` 的孔口是靠"面上的内环"这条途径找到的，原来的写法在**曲面/BSpline 面上**
   建出来的面不是好几何，切出来的布尔结果里含病态面 —— 表现为
   **下模能算出来、体积也对，但写 STEP 时被降级成 2 个 shell、"实体丢失"**，
   于是四件套 STEP 里只剩 3 个实体（BRepCheck 校验还是"有效"的，很有欺骗性！
   `ShapeFix`、缝合、`MakeSolid`、换 fuzzy、换 AP203/ManifoldSolidBrep 导出模式全都救不回来）。
   修法：`pipe_mold._planar_hole_plate` —— 改成"孔口包围盒 → **平面矩形** → 拉成棱柱"，
   几何一定简单、一定是平面，导出立即恢复正常（实测 4 个实体齐全 ✅）。
   排查这类问题的实用手段：导出后**把 STEP 读回来数实体**（`_pipe_diag/verify_mold4.py`）。
8. **`PartingError` 不接受 `stage=` 参数**（它内部固定 stage="parting_splitter"），
   传了会在错误路径上抛 `TypeError` 掩盖真实原因 —— 已清掉 `pipe_mold.py` 里的 5 处。

### 四.1 分模面高度必须夹在“材料厚度区间”内（贴模接触点夹紧）

只按侧影线取 h 还不够：侧影点可能落在**竖直面**上（例如管件侧壁、凸台的圆柱面），
这些点的 z 是一个区间，取中位数会跑到区间中部，于是分模面在凸台站位被抬得比台面还高，
下模就会在台面之上多出一层 1~2 mm 的“台阶”——产品顶出时必然干涉。

本程序的做法（`pipe_mold.silhouette_profile` + `choose_plane_height`）：

1. 用 `contact_face_flags` 找出**真正被模具成形的面**（面上取几个采样点，沿外法向偏移
   0.25 mm 落在模具实体内部 → 该面是型腔接触面；管件内孔面整面排除）；
2. **水平面模式**：取 `z_p = min(侧影最低点 − 0.2, 朝上接触面最低 z)`，并保证
   `≥ 逐列最低材料最大值 + 0.2`；
3. **侧影模式**：h 按站位取侧影中位，再用 `h ≤ min(朝上接触点 z)` 夹紧，
   夹紧限值在 **±2 站位邻域**内扩张，避免相邻站位之间残留薄台阶。

内置测试件（60×40×10 长方体 + Φ24×15 凸台）验证：
夹紧前分模面 25.0~31.8 mm（凸台处留台阶，顶出干涉 147 mm³），
夹紧后 25.0~30.0 mm，**三个方向干涉全部为 0.00 mm³**。

### 四.2 其它细节

* **三件套装配体**：`main.build_mold_assembly()` 把 [上模, 产品, 下模] 放进一个 compound
  （同一坐标系、按实际配合位置摆好），导出 `{prefix}_mold_assembly.step`；
  板件模式也会一并导出。Web 面板上有「⬇️ 三件套装配体」和「👁 看三件套装配」按钮。
* **凸台/圆柱与平面的“接缝内环”不是孔**：`_is_open_hole` 用“孔口中心沿面内法向
  偏移一点是否在产品实体之外”来区分真孔与接缝，避免对凸台根部误封盖。
* **孔口封盖两条途径**：① 长轴两端截面上的非最大环（管件内孔开口，本站件靠这条）；
  ② 面上的内环（侧面开孔）。两条互补，统计与诊断都会打印出来。
* **只保留主体**：切断内芯条后用“模芯角落参考点”判断哪个实体是主体
  （比逐块算体积快得多），其余内芯条丢弃 —— 这就是“外形包络型腔”。
* **诊断出图**：`_pipe_diag/plot_result.py <输出目录> <产品stp> [前缀] [x|y]`
  可把生成结果按截面画出来（红=上模、蓝=下模、黑=产品），用于人工核对分模。
* **核对装配体**：`_pipe_diag/verify_mold4.py <输出目录> <前缀>` 把导出的 STEP 读回来
  数实体、报各实体体积，并检查"上模+下模+产品+芯棒"与模芯的体积守恒
  —— 导 STEP 丢实体这类问题靠它一眼看出来。

### 四.3 第二个管件（`24TK_1324-31_ROTATE_x_CW_90deg`，661 × 50 × 66）实测

零件更小，全流程 **21 s**（`--no-verify --parallel`；外形包络布尔 7.6 s、侧影 4.1 s、分模 6.8 s）。

| 项 | 值 |
|---|---|
| 产品体积 / 模芯 | 121 304.5 mm³ / 700.95 × 90.34 × 106.37 mm |
| 孔口封盖 | 真孔 2 处（**端面截面 0 / 面内环 2**，走的是第②条途径） |
| 芯棒（内孔腔） | 568 253.9 mm³，611.2 × 45.7 × 60.2 mm |
| 自动推荐分模面 | z = **79.30**，安全区间 **[79.30, 21.75]** → **区间为空** ⚠️ |
| 逐列最低材料最大 / 朝上成形面最低 | 79.10 / 21.75 |
| 侧影随长轴变化 | 21.75 ~ 69.43 mm（**47.7 mm**，弯得很厉害） |

**结论**：这个件**没有可行的单一水平分模面**（`z_lo > z_hi`）——
件弯得太厉害，任何水平面都会在某处留台阶或在某处钩料。程序会明确提示，
此时应：① 用 `--parting-z` 自己挑一个折中高度，再用（不跳过）开模/顶出干涉校验确认能不能用；
② 或改用 `--parting-surface silhouette` 随形分模。

用户指定 `--parting-z 60` 的结果（`_pipe_diag/pz_run2`）：

```
孔口封盖 2 处 → 剔除内芯条 568 253.9 mm³
上/下模高度: 下模 60.00 mm（z 0.00 ~ 60.00）+ 上模 46.37 mm（z 60.00 ~ 106.37）= 模芯高 106.37 mm ✅
上模 2 816 380.4 mm³ ｜ 下模 3 228 263.9 mm³ ｜ 型腔模 6 044 823.0 mm³
四件套装配体: 4 个实体（上模 2 816 373 / 产品 121 305 / 芯棒 568 222 / 下模 3 228 167）✅
警告 2 条：① 指定的分模面超出安全区间；② 本次跳过了干涉校验
```

---

## 五、仍建议后续处理（未做，视需要）

* **缩水率/间隙**：目前型腔 = 产品外形（零间隙）。实际生产建议按材料缩水率
  放大产品（如 +0.5%），或给型腔留 0.05~0.1 mm 间隙。
* **斜切端面**：本件末端端面是斜切的，属于“朝下面高于朝上面”的局部倒扣征兆
  （8 个站位提示），水平/侧影分模面都靠“内孔填实 + 干涉检验”兜住了；
  若将来遇到真的卡模，可考虑把端面单独做滑块/镶件。
* **侧向孔**：贯通方向不是长轴的侧孔，若其孔口面没有内环、也不在端面，
  封盖无法自动定位，会保留内芯（此时校验会报警）。可扩展为"按孔轴线自动识别开口"。
* **并行布尔**：本工程为稳定性统一用 `run_parallel=False`，若要提速可评估开启。
